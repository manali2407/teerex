import { SelectBrandPipe } from './select-brand.pipe';

describe('SelectBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
